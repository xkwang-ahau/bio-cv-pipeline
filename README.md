# bio-cv-pipeline
A model-agnostic K-fold Stratified CV + retrain + test evaluation pipeline for PyTorch binary classifiers. Supports any architecture (MLP, CNN, GNN, Transformer, dual-tower) and data format (fixed vectors, variable-length sequences, PyG graphs, multi-modal).
